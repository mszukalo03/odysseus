# Session Log

> Part of odysseus/.project-knowledge/ | Last updated: 2026-07-27
> Append-only — never edit past entries.

| Date | Summary |
|------|---------|
| 2026-06-08 | Initial PROJECT_KNOWLEDGE.md created from code scan. Documented project structure, schema, all route files, env vars, systems, and features. |
| 2026-06-09 | Synced doc with the search provider registry refactor (commit 182bd15): documented `PROVIDER_REGISTRY`/`PROVIDER_FUNCTIONS`, the 4 new providers (Bing, Search1API, Firecrawl, Exa), dynamic `/api/search/providers` metadata, and new `*_api_key` settings. Uncommitted/in-progress: `research_handler.py` endpoint-probe timeout 15→60s & retries 1→2; `settings.js` hides result count when the active provider has no key. |
| 2026-06-09 | Q&A on search + deep research. Confirmed Exa is active (via stats `Search:` field). Documented how deep research works (pipeline + fixed `FINAL_REPORT_PROMPT` template + `research_max_tokens` cap) and why it ignores user output structure, search silent-fallback semantics + how to verify the used provider, and PDF capabilities (read/form-fill/browser-print only; no server-side text→PDF). Captured a 6-item Search & Deep Research improvement plan under Known Issues / TODOs. |
| 2026-06-09 | Added **Kimi (Moonshot)** as a first-class LLM provider (`slashCommands.js` setup + `providers.js`/`llm_core.py` endpoint labels; OpenAI-compatible, logo already present). Seeded **10 published starter skills** for owner `yahya` across productivity/research/memory/system via `SkillsManager` (files under `data/skills/`). Corrected the schema doc: skills are file-based `SKILL.md`, not a `skill_definitions` table; added the real `memories` table row. |
| 2026-06-11 | Completed Kimi integration by adding it to the UI provider dropdown (`index.html`) and bumping the service worker cache (`sw.js`). Synced the local `dev` branch with `upstream/dev` (bringing in 50+ commits) and resolved merge conflicts in `llm_core.py` and `slashCommands.js` to preserve both Kimi and upstream's NVIDIA providers. Pushed the resolved merge to `origin/dev`. |
| 2026-06-11 | **Avatar system**: chat bubbles now show username (instead of "You") via `window._currentUsername`. Added avatar element to user chat messages (initial letter or uploaded image). Click avatar in Settings > Account to upload (POST /api/upload → save file ID to /api/prefs/avatar_file_id). Avatar displays in sidebar, settings, and chat bubbles. Fixed FormData field name (`files`, not `file`) and response extraction (`data.files[0].id`). Replaced bare `showError`/`showToast` with `uiModule.` prefix. Added `has-avatar` class to hide `::before` dot. Existing chat bubbles refresh live after upload. |
| 2026-06-11 | **Navyseal theme**: new built-in theme `navyseal` (bg:#0a1628, fg:#b0cce8, panel:#0e1e36, border:#f0c800/YELLOW, red:#00bcd4/cyan). Default background pattern: aurora. |
| 2026-06-11 | **Neon glow toggle**: theme customize tab option to toggle pulsing box-shadow glow on all bordered elements (chat bubbles, input area, admin cards, modals, sidebar). Uses `body.neon-glow` class + `@keyframes neon-glow-pulse` animation on `--border` color. Saved in theme state alongside frosted. |
| 2026-06-11 | **6 new canvas background animations**: Aurora (slow-moving light curtains), Matrix Rain (falling katakana characters), Waves (undulating sine waves), Nebula (8 swirling gas blobs with HSL colors), Ripples (expanding concentric rings), Hex Grid (pulsing hexagons with staggered breathing). Registered in theme pattern system (`_BG_CLASSES`, `_CANVAS_PATTERNS`, dropdown options, canvas cleanup selector). Fixed hex grid row count (used `H/h` instead of `H/spacingY`) and added negative offset for full coverage. Increased hex visibility with fill+stroke and wider alpha/size pulse range. |
| 2026-06-11 | **RSS Feed Reader ("SmartRSS")**: New feature — 3-pane RSS reader with feed list sidebar, article list, and reader view. DB models: `FeedGroup`, `Feed`, `Article`, `FeedSyncAccount`. Backend: `services/feed/` (fetcher, discovery, OPML, full-content, YouTube resolver) + `routes/feed_routes.py` (full CRUD, articles, refresh, summarize, OPML). Frontend: `static/js/feedReader.js` + `.rss-*` CSS in `style.css`. Wired into `app.py`, `app.js`, `index.html` (nav rail + sidebar). YouTube channel URL resolution via `services/feed/youtube.py`. Window drag/snap/fullscreen via `makeWindowDraggable`. **Fixes**: CSP `img-src` missing `https:` blocked YouTube thumbnails/favicon → added `https:`; delete button added to feed items (always visible, right side); thumbnails switched from `<img>` to `background-image` for reliability. |
| 2026-06-11 | **YouTube video playback**: Replaced embed iframe approach (Error 153) with in-page modal overlay. Clicking ▶ opens a dark modal with YouTube embed + "Watch on YouTube" fallback link below. Embed still gives Error 153 for some videos — fallback link works (opens YouTube directly, counts views). Removed `window.open` popup approach (browser opens tab instead of popup). Added modal CSS, `_openVideoModal()` / `_closeVideoModal()` functions, event listeners for close button/backdrop/Escape. |
| 2026-06-12 | **Deep Research bugfix session**: Fixed 11 bugs across `deep_research.py` and `goal_based_extractor.py` identified through 3 audit iterations. Root causes: (1) `FINAL_REPORT_PROMPT` template override buried beneath essay defaults → moved STRUCTURE CHECK to top; (2) `_final_report` unreachable when synthesis fails → route findings through `_final_report` instead of early-exit via `_fallback_report`; (3) `_synthesize` timing out on full 500+ word prompt → pass condensed `research_plan` instead; (4) extractor reading user's process instructions as self-instructions → pass `research_plan` instead of full prompt as `goal`; (5) thinking tags leaking into extraction → "Output ONLY valid JSON" instruction; (6) `_classify_category` returning `None` for "general" → default to `"general"`. Also: expanded `_classify_category` prompt with per-category descriptions + examples; added domain-instruction to `QUERY_GEN_PROMPT`; added "General" category button to research panel (`panel.js`). All fixes verified against audit findings v1–v3. |
| 2026-06-13 | **RSS Feed Reader enhancements batch 1**: Nested groups (self-referential FK + tree UI), OPML import preserves group hierarchy, OPML dedup fix, OPML file upload, multi-select feeds with batch actions, group filtering via `group_ids` param. |
| 2026-06-13 | **RSS UI/UX fixes** (batch 2): (1) **Collapse/expand** — single-click group header toggles collapse, double-click selects group; toggle icon enlarged 12→18px with hover highlight; (2) **Select mode** — "☐" toggle button hides checkboxes by default, only visible when select mode is on; in select mode, clicking any feed item toggles its checkbox instead of navigating; (3) **Group AI summary** — `POST /api/feeds/groups/{group_id}/summarize` backend endpoint + "AI Summary" button in article list when a group is selected, shows digest in collapsible panel; (4) **Various** — feed item click in select mode toggles checkbox, `_groupSummary` state cleared properly across navigation. |
| 2026-06-19 | **RSS AI summary fix**: Added missing `query_llm()` to `src/llm_core.py` — both article and group summary buttons were silently failing with ImportError. Fixed group summary 504 by reducing prompt size (10 articles × 400 chars, 6000 cap, max_tokens=600). **RSS UX polish**: improved filter button sizing/hover states, active filter Navyseal fallback, article text sizes, feed icon softening, delete button hidden until hover, group-actions opacity transition (no layout shift), select-mode button moved to Groups header with icon+label, toolbar separator, auto-load All Feeds on open, uniform reader toolbar icon buttons. **Kimi**: added missing `<option>` to `#adm-epProvider` in `index.html`. Bumped SW cache to v334. |
| 2026-06-25 | **Upstream merge**: merged `upstream/dev` (20 commits) into local `dev`. Resolved 5 conflicts preserving RSS reader, deep research fixes, avatar, themes, Kimi, and 10-provider search registry. Upstream brought: llama.cpp auto-detection, admin share-defaults toggle, chat padding toggle, Gemma 4 12B catalog entries, vision fix, atomic APIKeyManager, 10+ bug fixes. **README update**: added RSS Feed Reader as its own feature bullet, expanded Extras to list canvas animations, neon glow, avatars, and all 10 search providers by name. |
| 2026-07-19 | **Migrated** `PROJECT_KNOWLEDGE.md` into `.project-knowledge/` folder structure (no code changes, doc reorg only). **Sync audit**: compared local `dev` (a3e18fd) against `odysseus-dev/odysseus` (= existing `upstream` remote, both at 68ba51c). Local is 301 commits behind, 12 ahead (RSS reader, deep research fixes, avatar, themes, Kimi, search registry — same local work logged 2026-06-11 through 2026-06-25). User requested a merge; working tree had uncommitted changes (`README.md`, old `PROJECT_KNOWLEDGE.md` modified, 5 test files deleted) — plan: finish this doc migration, commit the pending changes, then merge upstream's 301 commits into local `dev`. |
| 2026-07-19 | **Committed pending changes** (`e54d77a`): doc migration + dropped 5 already-locally-deleted Kimi/LM Studio test files + README sync. **Merged `upstream/dev`** (301 commits, `768b902`): 2 conflicts resolved — `static/sw.js` cache version (took v345, past both sides' bumps) and `tests/test_kimi_code_user_agent.py` (modify/delete, restored upstream's expanded version; the other 4 deleted Kimi/LM-Studio test files correctly stayed deleted per standard merge semantics). Full suite (4632 tests) had 3 post-merge failures, both from carrying local-only code across the merge: Bing search provider's leftover inline Mozilla UA, and a deep-research test stub missing the `research_plan` attribute our 2026-06-12 fix added. Fixed both (`abca91c`) — suite now fully green. **Discovered** several upstream structural refactors merged in: `tool_implementations.py` split into `src/tools/` + `src/agent_tools/`, route domains (contacts/gallery/history/memory/research) split into subpackages with backward-compat shims, new `src/model_capability_readers/` package, and a new `src/search/` package of unconfirmed purpose alongside the existing `services/search/` — flagged in [[roadmap]] for follow-up. Local `dev` and `upstream/dev` are now fully in sync. Updated `.project-knowledge/roadmap.md`, `structure.md`, `routes.md`, `history.md`, `INDEX.md` to reflect the merge. Considered adding a Claude Pro/Max subscription OAuth device-flow provider (cloning the existing ChatGPT-subscription/Copilot pattern) — decided **not to build it**: the OAuth client is scoped to Anthropic's own Claude Code CLI, and routing unrelated third-party traffic through it would violate Anthropic's consumer Usage Policy and risk the user's whole subscription. Fixed a real UI bug found in the process: `#welcome-screen .welcome-sub`/`.welcome-tip`/`.welcome-version` relied on flat low `opacity` (as low as 0.2), washing out to unreadable against busy canvas background patterns (e.g. hex grid) — replaced with `color-mix` against `--fg` plus a `text-shadow` halo derived from `--bg`, so contrast holds regardless of the active theme/pattern (`83ea72d`). Refreshed the README screenshot and feature list to match current state (provider count, llama.cpp auto-detection, background-animation count). Pushed everything to `origin/dev` (17 commits ahead of `odysseus-dev/odysseus:dev`, expected — includes the merge commit + 5 local commits on top of the 12 pre-existing local-only commits). **Backfilled `.project-knowledge/` from scattered repo docs** (`SECURITY.md`, `THREAT_MODEL.md`, `ROADMAP.md`, `docs/*.md`, `specs/architecture-runtime-inventory.md`, `tests/*.md`, `static/js/MODULE_SUMMARY.md`, `companion/README.md`, `integrations/*/README.md`) — added threat-model/auth internals, test-suite governance, security-CI, refactor-slice plan, attachment-reference schema, companion/Claude-Codex bridge routes, Outlook OAuth limitation, agent-migration manifest spec, and backup/restore tooling to the relevant files; resolved the previously-open "is `src/search/` used" question (partially — `core`/`providers` alias `services/search` via `sys.modules`, but 5 other files are still independent copies that can drift, tracked as #1058). |
| 2026-07-20 | **CI fix** (`da282e6`): Docker publish workflow failed with `invalid reference format: repository name (YahyaZekry/odysseus) must be lowercase` — GHCR/OCI requires lowercase image names but `github.repository` preserves case. Added a `prep` job that lowercases `github.repository` via `tr` and outputs it; `build`/`merge` jobs now depend on `prep` and use its output instead of `github.repository` directly. |
| 2026-07-20 | **RSS YouTube AI-summary feature**: `feedparser` leaves `content`/`summary` empty for many YouTube entries, so AI summary had nothing to work with. Added `_youtube_transcript_text()` (`routes/feed_routes.py`) — detects YouTube URLs, fetches the transcript via `services/youtube/youtube_handler.py`, falls back to it when content is empty (article + group summarize both use it, group fetches concurrently via `asyncio.gather`); `fetch_full_content` also branches to transcript fetch for YouTube URLs. New test file `tests/test_feed_routes_youtube_summary.py` (4 tests). |
| 2026-07-20 | **Fixed 3 reported RSS UI bugs**: chat input model-picker floating over the RSS/Notes modal (stray `z-index: 250` with no local stacking context — lowered to 2); wrong AI Summary icon (pencil → sparkle, 3 occurrences); AI Summary silently doing nothing on failure (no visible loading state + no `try/catch`, replaced with a loading panel + disabled button + real error toast). |
| 2026-07-20 | **RSS UI/UX enhancements**: added `j`/`k`/`m`/`s` keyboard shortcuts and Prev/Next toolbar buttons for article navigation; found and fixed a self-inflicted race condition where marking an article read triggers an async list refresh that could reshuffle articles mid-navigation (fixed via a `_readerNavList` snapshot taken when the reader opens); added an empty-snippet fallback ("▶ No description available") for video posts with no description; fixed a latent `_el()` bug where a compound CSS selector was passed to a `getElementById`-based helper. |
| 2026-07-20 | **Root-caused and fixed the real reason RSS AI-summary requests were failing on slow/hosted models**: it was never prompt size — `/api/feeds` was simply missing from `app.py`'s `_TIMEOUT_EXEMPT_PREFIXES` (unlike `/api/chat`, exempt for streaming), so every summarize request was killed at the global 45s `REQUEST_HARD_TIMEOUT` regardless of `query_llm`'s own timeout setting. Confirmed via a raw `curl` bypassing all app code (still timed out identically) that the model itself (`big-pickle` via OpenCode Zen) was simply slow, not broken. Fixed by exempting `/api/feeds/articles` + `/api/feeds/groups` from the hard timeout and raising `query_llm(timeout=...)` to 180s on both endpoints (matching existing deep-research precedent). Also found and fixed a second, unrelated bug this surfaced: `big-pickle` isn't in `_THINKING_MODEL_PATTERNS`, so its raw chain-of-thought reasoning (no `<think>` tag, no `reasoning_content` field) was leaking into and truncating summaries — fixed with an explicit "no reasoning" system-prompt instruction plus `max_tokens` 500/600→1200. Verified live end-to-end against the real model both times (long article: 125.7s server-side, succeeded; short article: clean 3-paragraph output, no leaked reasoning). Full suite: 4636 passed. |
| 2026-07-20 | **Committed** (`49b011f` fix + `79a58a5` docs) and pushed the whole RSS batch above to `origin/dev`. |
| 2026-07-20 | **RSS sidebar redesign, round 1**: fixed toolbar icons reading as disabled (shared `.doc-action-icon-btn` opacity 0.3 → scoped 0.6 for RSS), added the missing group-delete button (backend endpoint already existed, just never wired to UI — verified live, tested on a throwaway group), added `title` tooltips for truncated group/feed names, redesigned the search box (added icon + height, was using an unstyled shared base class) and filter/select buttons into a segmented-control look, and improved sidebar spacing/legibility. **Incident**: while live-testing the new delete button, 3 of the user's real (but empty) groups disappeared from the DB — cause not conclusively identified (code review shows no cascading-delete path, no access logs survived to confirm), but flagged transparently; verified no feeds/articles were lost (only empty group labels), user declined auto-recreation. See [[history]] for the full incident note and the stricter test-with-baseline protocol adopted afterward. |
| 2026-07-20 | **RSS sidebar redesign, round 2**: fixed the group action buttons (add-sub/rename/delete) being too small (~14×10px effective hit target) — enlarged to real 20×20px icon buttons; found and fixed the actual root cause of missing favicons (`fetch_feed()` never checked RSS 2.0's `<image><url>` — only Atom's `<icon>`/`<logo>` — so almost every non-YouTube feed fell back to a letter avatar; added the RSS 2.0 check plus a guessed `/favicon.ico` fallback with an `onerror` degrade); fixed the collapse/expand chevron not showing for groups with feeds-but-no-sub-groups (toggle logic only checked for sub-groups). |
| 2026-07-20 | **Added RSS drag-and-drop** (reorder feeds within a group, drag onto a different group to move): added `Feed.sort_order` column + migration (`_migrate_add_feed_sort_order`, same manual-`ALTER TABLE` pattern as `FeedGroup.parent_id`), `GET /api/feeds` now orders by `sort_order`, `sort_order` added to `update_feed`'s whitelist. Frontend wires a small grip handle on each feed row to the existing shared `dragSortModule` — no changes needed to that shared module, since the whole feed list already renders as one flat container with group headers and feed items interleaved by position; `_onFeedListReordered` infers each feed's new group from the nearest preceding header in final DOM order and persists via the same per-feed `PUT` loop already used by batch-move. Added a synthetic "Ungrouped" divider so ungrouped feeds have an explicit boundary (needed for correct group-inference, not just cosmetic). Verified end-to-end with a real simulated mouse drag (dispatched mousedown/mousemove/mouseup events) against the live DB, following the stricter baseline-snapshot-and-restore protocol from the earlier incident — confirmed correct persistence, then restored the test feed's original group/position exactly (diffed identical). Full suite: 4636 passed. |
| 2026-07-20 | **Committed and pushed** the sidebar redesign/group-delete/favicon-fix/drag-and-drop batch (`dcbc08c` code + `9163871` docs) to `origin/dev`. |
| 2026-07-20 | **Confirmed the SSRF `base_url`/`/api/v1/chat` roadmap item is already fixed** on current `dev` despite the tracking PR (#1039) being closed unmerged — full write-up in [[history]]. Updated roadmap/history accordingly. |
| 2026-07-20 | **Planned and implemented the remaining "can just do" RSS backlog** (6 items, via `/plan` — plan file `atomic-discovering-hamming.md`): (1) fixed the TTS button's broken `.speak()`→`.play()` API call; (2) documented why CSP `img-src` scoping isn't safely implementable (RSS aggregates arbitrary feed domains) rather than attempting it; (3) added a per-feed refresh-interval UI (⏱ icon, prompt-based edit, mirrors `_promptRenameGroup`); (4) added `action_refresh_due_feeds`, a new opt-in scheduled task (off by default) that auto-refreshes feeds whose interval has elapsed, reusing the existing `_refresh_single` helper wrapped in `asyncio.to_thread` per feed; (5) added infinite scroll to the article list (append-mode `_loadArticles`/`_renderArticleList`, switched article click handling to a single delegated listener); (6) fixed "can't drop a feed onto a collapsed group" from the earlier drag-and-drop work, via a capture-phase `stopImmediatePropagation` interceptor that expands collapsed groups on a first press (matching an existing pattern already used elsewhere in this codebase, e.g. `windowDrag.js`) rather than trying to re-render mid-drag (which would break `dragSort.js`'s assumption of a stable DOM during one gesture). All items independently verified live — including two real-mouse-drag tests and a direct invocation of the new scheduled action — with the baseline-snapshot-and-restore protocol used for any test that touched real data. Full suite: 4636 passed throughout. Nothing committed until this project-knowledge update. |
| 2026-07-24 | **RSS proposed upstream as a new feature via GitHub issue** — user clarified the whole RSS Feed Reader was their own original work, not upstream's, so pivoted from "split into per-feature PRs" to opening a single issue (per `CONTRIBUTING.md`'s explicit no-bulk-agent-PR policy) describing it as a proposed feature: [#5688](https://github.com/odysseus-dev/odysseus/issues/5688). Awaiting maintainer response before any PR. |
| 2026-07-24 | **Added a fixed-port run script + systemd service scaffolding** for "always on" deployment: `scripts/run-odysseus.sh` (port 24950, sets `APP_PORT`), fixed `install-service.sh`'s missing executable bit. `odysseus-ui.service` filled in with the user's real username/port so they can test-install it themselves (`./install-service.sh`, needs sudo — left for the user to run, not yet confirmed done); must be reverted to the `YOURUSER` placeholder before any commit, per the user's explicit instruction — tracked in [[roadmap]]. |
| 2026-07-24 | **Restructured the sidebar into AI & Knowledge / Personal / Appearance categories** (user: "it feels scattered... some things are for ai, some things are personal like email and rss"), with Email genuinely nested inside Personal rather than left in the old flat Tools list. Follow-up fix: section header titles were the same font/size/color as the `.list-item` rows under them, making categories hard to visually separate — bumped to bold + uppercase + letter-spaced globally. Committed as `b205f82` (bundled with the run-script/systemd work above) — this commit predates today's project-knowledge update, hence logged retroactively here. |
| 2026-07-24 | **Redesigned Email into a 3-pane webmail layout** — user shared a reference screenshot of an Outlook-style client and explicitly chose the full-rewrite option over a lighter restyle. Backend: `GET /api/email/folders` gained a sibling `unread_counts` dict (via IMAP `STATUS ... (UNSEEN)`, additive — doesn't touch the existing `folders` array shape). Frontend: `openEmailLibrary()`'s modal template rewritten into `.email-pane-folders`/`.email-pane-list`/`.email-pane-reader`; `_createCard`/`_toggleCardPreview` split into pure-data + new-DOM-target versions (`_createEmailRow`, `_loadReadingPane`) reusing all existing wiring (attachments, inline images, reply/AI-reply/translate, stale-response guards) rather than rewriting it; old card-based code left in place but unreferenced (shared CSS with Documents/Memory makes removal a separate follow-up). Verified live against a real Gmail account (89 messages) across every preserved feature — folder switch, search, filter, bulk-select, compose, reply, the "More" menu (found and fixed 2 real null-reference bugs there from the `card`→`null` transition), Scheduled folder — plus the full backend suite (4639 passed, 0 failed). Found and fixed a real gap during mobile verification: the CSS for single-pane mobile collapse existed but nothing in JS ever added the pane-visibility classes, so mobile rendered completely blank — added `_setMobilePane()` + back buttons, plus fixed a related bug where re-tapping the already-active folder stranded mobile users on the folder pane. |
| 2026-07-24 | **Added folder-sidebar collapse + drag-resizable panes to the Email 3-pane layout**, and fixed the "Personal" sidebar category's nested items (Email/Calendar/Notes/Tasks/RSS Feeds) reading as peer categories instead of children — both requested together ("we need some margin here... need to collapse the sidebar and move the bars to extend/shorten... the side bar needs to be better in showing the categorize groups"). Email: added a collapse toggle (forces `width:0 !important` — found and fixed a real bug where a drag-resize-saved inline width could otherwise block it) and drag handles between panes with `localStorage`-persisted widths; added `padding: 6px 10px 0` to the message-list pane (previously flush against the pane edges) — caught and fixed a mobile-width regression this introduced (pushed Select/refresh buttons off-screen at 390px) by scoping a smaller padding inside the mobile media query. Sidebar: wrapped Email/Calendar/Notes/Tasks/RSS Feeds in an indented `.section-nested-group` with a left guide-line, and toned the nested Email header back down to plain title-case so it stops competing visually with real category headers. Full details in [[history]]. |
| 2026-07-24 | **Committed and pushed**: discovered — right before pushing — that `odysseus-ui.service`'s real username/path (from the run-script/systemd work earlier this session) had already been committed *and pushed* in `b205f82`, despite the explicit instruction not to. Asked the user how to proceed; they chose to fix it and push. Reverted the placeholder in its own commit (`19aeec5`, kept the legitimate port/host/env-var improvements from `b205f82`) — the real username remains visible in `b205f82`'s history since a full scrub wasn't requested (would need a history rewrite/force-push). Committed the Email 3-pane redesign + sidebar nested-group fix separately (`236ef91`). Both pushed to `origin/dev`. |
| 2026-07-25 | **Follow-up polish round on the Email 3-pane + Theme, from live user feedback**: (1) fixed the Email folder-sidebar collapse hiding icons entirely — user compared it unfavorably to the main app sidebar's own collapse (which keeps an icon rail); changed the collapsed state to a 40px icon-only rail with active-state, unread dots, and hover tooltips still working; (2) fixed a Theme → Customize tab layout bug ("neon glow is out of the container") — root cause was `.theme-fd-row` packing 5 groups with no `flex-wrap`, so the Frosted/Neon-glow toggles got pushed past the card's border instead of shrinking; added `flex-wrap: wrap` + `min-width: 0`; (3) built a new-mail pulse banner feature — `GET /api/email/unread-state` gained a `latest` field (sender/subject of the newest message, from the already-cached local index, no extra IMAP call), and `emailInbox.js`'s existing 60s unread-poll now fires a top-right glowing banner the first time a given `max_uid` is seen (deduped via a session-only variable, skipped while the inbox is already open). Verified the banner's trigger/dedup/content logic via a console-log trace and direct DOM inspection after several rounds of trying (and failing) to catch its exact 8-second-delayed lifecycle via live screenshot through the remote preview bridge — the round-trip latency between tool calls kept swallowing the whole window, a testing-environment limitation rather than a code issue. Also confirmed the systemd service (a lingering TODO) is installed, enabled, and running. Full suite: 4639 passed, 3 skipped. |
| 2026-07-26 | **Third polish round on the Email reading pane, from live user feedback**: (1) fixed folder-badge unread counts being unreadable — a generic `.list-item span` rule (specificity 0-1-1) was silently beating the badge's own single-class color/font-size rule (0-1-0); rescoped to `.list-item .email-folder-badge` (0-2-0); (2) confirmed and fixed a pre-existing, app-wide bug the user asked about directly — `.modal-content` hardcoded Inter font regardless of the Theme → Customize Font setting, while the rest of the app respected it; switched to the same `var(--font-family, ...)` fallback chain, verified live by actually switching to Sans-serif and back; (3) user flagged the reader's 6-button action row as "crumbled" — a first pass only widened the narrow-breakpoint tile/label by 4px to stop "Reply all" truncating to "Reply a...", but the user came back saying it was still cramped; redesigned properly to icon-only buttons in a single row (title tooltips carry the names), cutting the cluster's height by more than half; (4) consolidated Reply/Reply All/Reply with AI into one dropdown off the Reply button per direct request, reusing the existing "More" menu's dropdown styling/dismiss helpers rather than building a new one — down to 4 top-level buttons from up to 6. Scoped all of this to the active 3-pane reading pane only; 3 other duplicated header blocks elsewhere (dead code + two less-used "open as tab/window" views) were left untouched. CSS/JS only, no backend changes — full test suite not re-run since nothing server-side changed. |
| 2026-07-26 | **Diagnosed and fixed a real agent-mode bug reported by the user, found in Odysseus's own chat data (not a Claude Code CLI session)** — user asked to check an in-app conversation titled "Installing Anthropic Claude Code"; first misread this as a Claude Code CLI transcript and gave a wrong, confabulated answer (owned the mistake once corrected). The actual conversation was an odysseus **agent-mode** session (`sessions` table, id `35391cf2...`, model `big-pickle`) where the user pasted terminal output about a blocked npm postinstall script, and the agent's own bash tool repeatedly tried to run the exact `sudo npm install...` command itself — 3 separate times across the conversation, including immediately after apologizing for doing exactly that — always failing with "a terminal is required to read the password" since the sandbox has no TTY. User clarified they wanted the agent to actually succeed (not just fall back to "paste this yourself"), since that's the point of agent mode vs. plain chat. Traced the root cause to `agent_loop.py`'s `TOOL_SECTIONS["bash"]` prompt: it already documents the no-TTY sandbox limit for `input()`/`curses`/`termios`/etc. but never extended that same limit to `sudo`/password prompts, and never told the model to stop retrying an identically-failing command. Also found: this machine's `npm config get prefix` is already user-owned (`~/.npm-global`), so `sudo` was never actually necessary for this specific install — extended the prompt to tell the agent to check for and prefer the no-sudo path first. Full suite: 4639 passed, 3 skipped (prompt-only change, doesn't verify the model's actual behavior changes). |
| 2026-07-27 | **Made agent mode actually able to run `sudo`, and found a pre-existing SSE bug along the way.** Follow-up to yesterday's prompt-only fix — the user pushed back that they wanted the agent to *succeed* at sudo, not fall back to "paste this in your terminal", since that's the whole point of agent vs. chat mode. First corrected a misconception (mine and the model's): agent mode already runs on the user's real machine as their real user, not an isolated sandbox. Then fixed three gaps: (1) `BashTool` never passed stdin, so sudo had nowhere to read a password — now it probes `sudo -n true` first (skipping the prompt when passwordless sudo already works), otherwise blocks and asks the browser via a new in-memory broker, then runs `sudo -S -p ''` with the password piped in; (2) `HOME` was force-overridden to the agent's data dir, which was the *actual* root cause of the original npm episode — with the real `HOME` restored, npm resolves to the user-owned `~/.npm-global` and the original install needs no sudo at all; (3) **`tool_progress` was missing from the SSE relay whitelist in `chat_routes.py`**, so the agent loop emitted those events and the chat route silently dropped every one — a pre-existing bug that also explains why the live elapsed-time/output-tail on a running tool card never displayed anything. Debugging that took several rounds: instrumented `JSON.parse`, then tee'd the raw `fetch` stream to prove the frames genuinely weren't being sent; also hit a stale service-worker cache (`odysseus-v357`) and a poisoned conversation history where the model kept refusing to even try sudo, both of which masked the real issue. Verified end-to-end live: prompt appears mid-turn with the exact command, Cancel unblocks the backend and returns a clean message. Success path proven with a stub `sudo` rather than handling the user's real password. New: `src/sudo_auth.py`, `routes/sudo_routes.py`, `static/js/sudoPrompt.js`. Full suite: 4639 passed, 3 skipped. |
| 2026-07-27 | **Immediately after the sudo work, the agent started reporting it had no shell tool at all** — user retried `run sudo id -un` and got a list of only web_search/web_fetch/manage_memory/ask_user/update_plan. This turned out to be true rather than a hallucination: Odysseus selects a per-turn toolset, `ALWAYS_AVAILABLE` is deliberately tiny (3 tools) so `bash` is only reachable via ChromaDB semantic retrieval or the `files` domain — and **ChromaDB is down on this install**, so selection fell through to `_KEYWORD_HINTS`, which had no `bash` mapping whatsoever (`Keyword fallback selected: []` in the logs, a line I'd seen earlier without recognising its significance). Added a concrete shell/command keyword hint → `{bash, python, manage_bg_jobs}`, avoiding bare verbs like "run" that would false-positive on "run a search". Verified 8 shell phrasings now select bash (including the exact failing one) with no false positives on search/research/email/calendar, then confirmed live that `run sudo id -un` reaches the password modal. Also logged the underlying ChromaDB outage as its own roadmap item, since it degrades semantic retrieval for every domain, not just shell. |
| 2026-07-27 | **Sudo prompt confirmed working end-to-end with the user's real password.** After the shell-keyword fix, the user retested `sudo id -un` in the shared dev-preview window and reacted with anger — the visible result was "sudo prompt was cancelled or timed out", which read as still broken. Root cause: an earlier automated test had run in that exact shared window and clicked Cancel programmatically to prove that path, leaving only the post-cancel text on screen with no modal — a real self-inflicted confusion, not a code bug. Checked `journalctl` for the systemd service against file mtimes to rule out a stale-server theory first (it wasn't that — the systemd instance had zero record of the request; it was the shared preview window). Sent a fresh command and, this time, left the modal alone. User typed their actual password and clicked Authorize themselves: `sudo id -un` returned `root`. That's the real proof the earlier stub-`sudo` test couldn't provide. Also flagged as its own note: the systemd service (port 24950, "always on" instance) has been running since 2026-07-25 with no reload and has none of the last three days' fixes loaded — needs a manual restart whenever the user wants this live outside the dev preview. |
| 2026-07-27 | **Second sudo gap found and fixed: wrapper scripts.** User restarted the systemd service themselves and re-tested on a fresh process with every prior fix loaded — `garuda-update` still silently failed asking for a password with no UI prompt. Traced to `_mentions_sudo()` only matching the literal word "sudo" in the command text; `garuda-update` is a separate script that calls sudo internally, so the whole prompt path was skipped and it just failed raw with "a terminal is required to read the password". Also caught the "garuda update"/"pacman"/"system update" phrasing gap in `_KEYWORD_HINTS` (added alongside). Fixed the deeper issue with a real pty: on any bash failure matching sudo's own tty-required error text, request a password through the same broker as before, then retry the exact command over `pty.openpty()` (with `preexec_fn` doing `setsid()`+`TIOCSCTTY`) so the *internal* sudo call gets a controlling terminal to prompt on — no need to know how the wrapper invokes sudo. Verified against a stub tty-checking script (not real sudo, consistent with never handling the user's actual password myself): correct password succeeds with output redacted, wrong password fails and clears the cache, ordinary commands are unaffected. Full suite green (4639 passed, 3 skipped, no new hangs). |
| 2026-07-27 | **Third sudo-related gap: garuda-update's own confirmation prompt, on a pty we can't reach.** After the pty-based sudo fallback shipped, the user's next real `garuda-update` run got past the password step but hung forever at pacman's "Proceed with installation?" — because garuda-update re-execs itself twice (sudo, then systemd-inhibit), and one of those hops allocates its own fresh pty, orphaned from the one our code opened. Confirmed via live `ps`/`/dev/pts` inspection, including proving that writing to the pty device node directly doesn't work either (it's delivered as output to the master reader, not input to the child). Read garuda-update's actual source instead of guessing further, found it has a real `--noconfirm` CLI flag that survives every re-exec hop via `"$@"`, and rewrote the command to always include it. This is a standing, security-relevant change (auto-confirms all future garuda-update prompts) — the permission classifier correctly blocked the first attempt since the user's "same thing" reply wasn't explicit enough authorization for a persistent behavior change; asked directly via AskUserQuestion and got an explicit yes before implementing. Suite green, pushed (`51e9705`). |
| 2026-07-28 | **Root-caused the day-long "no bash tool" bug properly and stopped patching symptoms.** Two prior rounds of keyword-hint additions hadn't fixed it; the trigger that exposed why was `"cool check"` — a context-dependent follow-up with zero shell keywords, which no wordlist can ever match (and ChromaDB is still down, so there's no semantic fallback). Moved `bash`/`python`/`manage_bg_jobs` into `ALWAYS_AVAILABLE` so availability isn't decided by string matching at all, keeping the deliberate workspace carve-out intact (a test caught that the blanket change had trampled it). Verified live end-to-end this time rather than just asserting the schema: keyword fallback empty, `bash` still sent, agent actually invoked it and reported a real background job's output. Also fixed pacman's ANSI escape codes leaking as raw garbage into both the UI and the model's context now that the pty path makes tools emit colour. Process notes from this session, both earned: I'd been committing+pushing after every incremental fix off a single early approval, which left the repo history messy — stopped, and won't commit without explicit per-change confirmation; and I'd twice claimed "verified" off unit tests or a tool-schema check when the actual reported failure was behavioural, which is not the same thing. |
